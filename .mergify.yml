pull_request_rules:
  - name: comment with title
    conditions:
      - label=test-template
    actions:
      comment:
        message: "PR Title: {{title}} | Author: {{author}} | Body: {{body}}"
# proxy-write-test-by-B
