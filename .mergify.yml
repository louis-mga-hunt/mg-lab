queue_rules:
  - name: default
    merge_method: merge-batch
    batch_size: 2
    merge_conditions:
      - check-success=slow-ci

pull_request_rules:
  - name: queue on label
    conditions:
      - label=queue-me
    actions:
      queue:
        name: default
