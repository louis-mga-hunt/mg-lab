queue_rules:
  - name: default
    merge_conditions: []
    queue_branch_prefix: "../"
pull_request_rules:
  - name: queue it
    conditions:
      - base=main
      - label=do-queue
    actions:
      queue:
        name: default
